<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>딱 걸리뷰 | 주문서</title>

    <!-- Bootstrap CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>

    <!-- jquery 라이브러리 -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</head>
<body>
    <div class="container">
        <br>
        <h1>주문서<span><a href="./delivr_order.html" class="btn btn-outline-warning">돌아가기</a></span></h1>
        <p>주문에 필요한 정보를 입력해주세요</p>

        <br>
        <label for="adminid" class="form-label">사용자 인증서 ID:</label>
        <input type="text" id="id" class="form-control">
        <br>
        <label for="adminid" class="form-label">주문할 곳:</label>
        <input type="text" id="host" class="form-control" value="">
        <br>
        <label for="adminid" class="form-label">주문 내용:</label>
        <input type="text" id="" class="form-control">
        <br>
        <div class="form-check">
          <input class="form-check-input" type="checkbox" value="participating" id="status">
          <label class="form-check-label" for="flexCheckDefault">
            리뷰 이벤트 참가 여부
          </label>
        </div>
        <br>
        <button id="btn" class="btn btn-outline-warning">생성</button>
        <br><br>
    </div>
</body>
<script>
    $('#btn').click( ()=> {
      // parameter가져오기
      var id = $('#id').val()
      var 
      // /admin post 요청하기
      $.post('/order', {adminid, passwd}, (data)=>{
        if(data.result == 'success'){
            alert("성공했습니다.");
        }else {
            alert("실패했습니다.");
        }
        // 응답 표시하기
        $('#result').empty();
        $('#result').append("<p>"+JSON.stringify(data)+"</p>");
      });
    });
  
  </script>
</html>